# 图片模型提示词插件 README

## 项目简介

这是一个为 Qwen、Z-Image 等图片生成模型设计的提示词管理插件。它集成了图片描述的所有常见分类，通过选择方式录入提示词，并收集了每个分类对应的常见值。插件每个分类默认值为空但可选择随机生成，同时提供前置和后置手动输入区域。新增 NSFW 选项分类以满足不同内容需求。

## 主要功能

### 1. 分类化提示词管理
- 集成图片描述的所有常见分类（如风格、主题、构图、光照、色彩等）
- 每个分类提供预设的常见值选项供选择
- 分类默认值为空，避免强制填充

### 2. 智能录入方式
- 通过下拉选择、复选框等界面元素录入提示词
- 减少手动输入错误，提高提示词构建效率

### 3. 随机生成选项
- 每个分类保留“随机”选项
- 随机从该分类的常见值库中选择一个值
- 增加生成结果的多样性和探索性

### 4. 手动输入区域
- **前置提示词**：在分类提示词前添加自定义内容
- **后置提示词**：在分类提示词后添加自定义内容
- 保持灵活性，满足个性化需求


### 5. NSFW 内容选项
- 新增 NSFW（Not Safe For Work）分类选项
- 包含相关的内容级别和风格选择
- 明确标识，避免误用
## 界面  
<img width="1119" height="718" alt="image" src="https://github.com/user-attachments/assets/9b59c6c1-18d2-431f-8c4a-a486218332fe" />



## 数据结构

插件使用 JSON 格式存储提示词配置，基本结构如下：

```json
{
  "主体 (Subject)": {
    "人物 (People)": [
      "单人 (Single Person)",
      "双人 (Couple)",
      "多人 (Group)",
      "儿童 (Child)",
      "老人 (Elderly)",
      "男性 (Male)",
      "女性 (Female)",
      "男性特写 (Male Portrait)",
      "女性特写 (Female Portrait)",
      "全身 (Full Body)",
      "半身 (Half Body)",
      "背影 (Back View)",
      "侧脸 (Side Face)",
      "正脸 (Front Face)"
    ],
    "动物 (Animals)": [
      "猫 (Cat)",
      "狗 (Dog)",
      "鸟 (Bird)",
      "马 (Horse)",
      "鹿 (Deer)",
      "龙 (Dragon)",
      "独角兽 (Unicorn)",
      "恐龙 (Dinosaur)",
      "昆虫 (Insect)",
      "鱼 (Fish)"
    ]
```


## 安装与使用

### 安装方式
1. 下载插件文件到本地
2. 根据目标平台（如 Stable Diffusion WebUI、ComfyUI 等）的插件安装方式集成
3. 重启图片生成工具
   
# 安装与依赖

## 唯一依赖
本插件仅有一个必需的外部依赖库：
*   **PySide6==6.7.2**：用于构建图形用户界面（GUI）的 Python 绑定库。

## 在 ComfyUI 中安装插件及依赖的方法

### 方法一：通过 pip 手动安装依赖（推荐）
1.  将插件文件夹（例如 `prompt_manager`）复制到 ComfyUI 的 `custom_nodes` 目录下。
2.  打开终端（命令行），**确保已激活 ComfyUI 的 Python 环境**。
3.  使用 pip 安装指定版本的 PySide6：
    ```bash
    pip install PySide6==6.7.2
    ```

    如果使用 ComfyUI 启动器，其 Python 解释器通常位于 `python_embeded` 文件夹中，命令应为：
    ```bash
    path_to_comfyui/python_embeded/python.exe -m pip install PySide6==6.7.2
    ```

4.  重启 ComfyUI。

### 方法二：通过 requirements.txt 文件安装
如果插件提供了 `requirements.txt` 文件（内容为 `PySide6==6.7.2`），可以将其放入插件目录，然后在 ComfyUI 根目录下执行：
```bash
pip install -r custom_nodes/prompt_manager/requirements.txt
```

**注意**：直接安装可能会与 ComfyUI 的核心依赖冲突，建议先确认环境。

### 方法三：使用 ComfyUI 管理界面（如果支持）
某些 ComfyUI 启动器或管理工具提供了图形化界面来安装 Python 依赖库。您可以：
1.  进入“Python 依赖管理”或类似界面。
2.  点击“安装新库”。
3.  输入 `PySide6` 并指定版本 `6.7.2`，然后点击安装。

## 安装验证
安装完成后，可以在 Python 环境中运行以下命令验证是否安装成功：
```python
import PySide6
print(PySide6.__version__)  # 应输出 6.7.2
```


## 故障排除
*   **`ModuleNotFoundError: No module named 'PySide6'`**：表示 PySide6 未正确安装。请确保在正确的 Python 环境中执行了安装命令。
*   **版本冲突**：如果已存在其他版本的 PySide6，可以尝试先卸载再安装：`pip uninstall PySide6`，然后重新安装指定版本。
*   **系统依赖问题**：在 Linux 系统上，PySide6 可能需要额外的系统库（如 `libxcb-cursor0`）。如果遇到 Qt 平台插件错误，可能需要安装这些系统包。

### 基本使用流程
1. 打开插件界面
2. 根据需要选择各个分类的提示词值
3. 开启/关闭随机选项
4. 在前置/后置输入区域添加自定义内容
5. 点击生成，插件会自动组合所有提示词
6. 结果可保存到历史记录或导出为 JSON

### NSFW 选项使用说明
- 该选项默认设置为“none”（无 NSFW 内容）
- 如需生成相关内容，请明确选择相应级别
- 请确保使用环境允许生成此类内容
- 建议在合适的场景和受众下使用

## 分类示例

插件包含但不限于以下分类：

| 分类 | 示例值 | 说明 |
|------|--------|------|
| 风格 | 写实、动漫、油画、水彩、像素艺术 | 图像的整体艺术风格 |
| 主题 | 肖像、风景、动物、建筑、奇幻 | 图像的主要内容 |
| 构图 | 特写、全身、三分法、对称 | 元素的排列方式 |
| 光照 | 自然光、工作室光、背光、黄金时刻 | 光线效果 |
| 主体 | 人物、动物、物品| 色彩方案 |
| 画质 | 4K、高清、胶片颗粒、细节丰富 | 图像质量描述 |
| NSFW | 无、随机、 | 成人内容级别 |

## 配置说明

### 默认值设置
- 所有分类初始状态为空选择
- 用户必须主动选择或启用随机才会添加该分类提示词
- 避免生成不需要的分类词汇

### 随机选项逻辑
- 启用后，每次生成从该分类的选项库中随机选择一个
- 可与其他固定选择分类混合使用
- 支持批量启用多个分类的随机选项

### 提示词组合规则
1. 前置提示词（如存在）
2. 各分类选中的提示词（按固定顺序排列）
3. 后置提示词（如存在）
4. 所有部分用逗号分隔，形成最终提示词



## 自定义与扩展

### 添加新分类
1. 在分类配置文件中添加新分类
2. 定义分类名称和选项数组
3. 插件界面会自动显示新分类

### 修改选项库
1. 编辑对应分类的选项数组
2. 支持添加、删除或修改选项值
3. 更改后需要重新加载插件

### 调整界面布局
1. 通过修改 CSS/样式文件调整外观
2. 支持响应式设计适配不同屏幕
3. 可调整分类的显示顺序

## 注意事项

1. **兼容性**：确保插件版本与图片生成工具版本兼容
2. **性能**：分类和选项过多可能影响加载速度，建议合理组织
3. **存储**：历史记录和配置会占用本地存储空间
4. **更新**：定期检查分类选项库的更新，获取最新的提示词趋势
5. **NSFW 内容**：请负责任地使用该功能，遵守相关法律法规和平台政策

## 版本历史

- v1.0.0 (2026-04-03)：初始版本发布，包含基本分类、随机选项、历史记录和导入导出功能
- v1.0.1 (2026-05-02)：分类分组

## 技术支持与反馈

如遇到问题或有功能建议，请通过项目仓库的 Issues 页面提交。

---

**免责声明**：本插件仅为提示词管理工具，不保证生成内容的质量或适宜性。NSFW 功能请在使用前确认符合当地法律法规和平台政策。使用者应对生成的内容负全部责任。
